apiVersion: v1
kind: Namespace
metadata:
  name: projectsveltos
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: projectsveltos
  labels:
    control-plane: clusterinventory-controller
spec:
  selector:
    matchLabels:
      control-plane: clusterinventory-controller
  replicas: 1
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        control-plane: clusterinventory-controller
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - command:
        - /manager
        args:
        image: controller:latest
        name: manager
        ports:
         - containerPort: 8443
           name: metrics
           protocol: TCP
         - containerPort: 9440
           name: healthz
           protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - "ALL"
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: healthz
            scheme: HTTP
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /readyz
            port: healthz
            scheme: HTTP
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 10m
            memory: 128Mi
      serviceAccountName: controller
      terminationGracePeriodSeconds: 10
